<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.airportbus.bus.mapper.SearchHotnessMapper">

  <select id="selectAirportCodeByBusSourceId" resultType="java.lang.String">
    SELECT a.code
    FROM bus_route b
    JOIN airport a ON a.id = b.airport_id
    WHERE b.source_id = #{sourceId} AND b.deleted = 0 AND a.deleted = 0
  </select>

  <select id="selectAirportIdByCode" resultType="java.lang.Long">
    SELECT id FROM airport WHERE code = #{code} AND deleted = 0
  </select>

  <!-- 按天累加:唯一键 (airport_id, day, deleted) 命中则 cnt += delta -->
  <insert id="upsertStat">
    INSERT INTO airport_search_stat(airport_id, day, cnt)
    VALUES(#{airportId}, #{day}, #{delta})
    ON DUPLICATE KEY UPDATE cnt = cnt + #{delta}
  </insert>
</mapper>
