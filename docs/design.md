# Design: 全球机场巴士信息(Global Airport Bus Information)

Generated by /office-hours on 2026-06-11
Branch: unknown (尚未 git init)
Repo: Global-Airport-Bus-Information-Fullstack
Status: DRAFT
Mode: Builder

## Problem Statement

一个 Web 网站,用户按「国家 / 城市 / 机场」搜索,展示当前的机场巴士信息(线路、班次、价格、停靠站、提醒、图片、文件)。同时提供完整用户系统(注册/登录、个人中心、收藏、站内信、建议工单)、界面多语言、以及一个管理后台(用户统计、订阅统计、巴士信息维护、操作记录)。

核心联动:当某条被收藏/订阅的巴士信息更新时,系统及时通过站内信推送给相关用户。

数据源当前只覆盖维也纳、上海两市;服务端「主动抓取数据源」暂不实现,用 [data.json](https://github.com/fupengfei/Global-Airport-Bus-Information/blob/main/data.json) 作为初始化数据。数据变更由管理员在后台维护触发。

## What Makes This Cool

不是又一个静态信息站。真正的亮点是**「订阅 + 变更推送」的闭环**:用户收藏一条线路 = 订阅;管理员改动那条线路 → 系统自动算出变更、找到订阅者、生成站内信、实时推送。把一个「查询站」变成一个「会主动通知你」的工具,这是从一次性使用到持续黏性的关键。

## Constraints

- 后端:Spring Boot 3 + Spring MVC + MyBatis + MySQL + Redis(已定)
- 前端:Vue 3 + TypeScript + Vite + Element Plus(本次决策)
- 数据仅维也纳、上海两市;服务端抓取延后,data.json 做种子数据
- RESTful 接口;前后端分离
- 多语言界面

## Premises(已确认)

1. **架构用模块化单体,不用微服务。** —— 已确认。两个城市的数据、读多写极少的 CRUD + 站内信,微服务带来的服务发现/服务间通信/分布式事务/独立部署成本远大于收益。单体内按模块清晰分包,边界划清,将来真有流量任何模块可干净拆出。
2. **data.json 做初始化种子数据,服务端抓取延后。** —— 已确认。
3. **「推送」的真实触发源是管理员保存动作,不是真实抓取。** —— 已确认。所谓「数据源变更」= 管理员在后台改了某条巴士信息。
4. **收藏即订阅。** —— 推定。spec 中「订阅/收藏用户」混用,本设计将「收藏一条巴士线路」直接等同于「订阅该线路的变更通知」,不单独做订阅实体。若你要更细粒度(收藏≠订阅),见 Open Questions。
5. **多语言主要是界面文案(i18n),巴士数据本身保留原文。** —— 推定。维也纳站名是德语、上海是中文,数据是地域性的;先不强求数据翻译,预留可选译文字段。

## Approaches Considered

### Approach A: 模块化单体(已选)
单个 Spring Boot 应用,内部按业务模块分包,模块间只通过接口/Service 调用。
- Effort: M
- Risk: Low
- Pros: 开发快、一次部署、本地调试简单、事务在单库内天然一致(站内信联动无需分布式事务)
- Cons: 所有模块共享一个进程,极端扩展时需先拆分(但边界已留好)
- Reuses: 标准 Spring Boot 分层

### Approach B: 微服务(原始需求,已否决)
拆 gateway + bus-service + user-service + message-service + admin-service,各自独立库与部署,Nacos/Eureka 注册中心。
- Effort: L/XL
- Risk: High
- Pros: 贴合原始需求描述;理论上各服务独立扩展
- Cons: 站内信联动需跨服务 + 分布式事务/最终一致;运维编排重;对两城数据量是过度设计
- Reuses: Spring Cloud 全家桶(但引入大量基础设施负担)

### 前端:Vue 3 + Element Plus(已选) vs React + Ant Design
两者都能做。选 Vue 3 + Element Plus:与 Spring Boot/中文社区生态贴合、Element Plus 后台表格/表单/权限菜单成熟、上手快。React + AntD Pro 生态更大,作为备选保留。

## Recommended Approach(工程设计)

### 模块划分(单体内分包)

```
com.airportbus
├── bus       国家/城市/机场/巴士线路 查询 + 维护;data.json 初始化
├── user      注册/登录/JWT/个人中心/收藏(=订阅)
├── message   站内信 + 变更推送
├── ticket    建议工单 + 回复
├── admin     后台:用户统计、订阅统计、巴士维护入口
├── audit     操作记录(管理端写操作全量审计)
└── common    异常、统一响应、i18n、安全、Redis 配置
```

### 数据模型(MySQL,源自 data.json)

> **自然键原则(对抗评审 P0)**:data.json 里 `bus` 有业务字符串 ID(如 `"vie-vab1"`),country/airport 有 `code`。**必须保留这些自然键并加唯一索引**,否则种子导入器无法做幂等 upsert(重跑就插重复数据)。下面每张表都显式标了唯一键和外键。所有自增主键统一 `BIGINT`;字段类型已按 data.json 真实值定死。

巴士数据(读多写少,整棵树适合 Redis 缓存):
- `country(id, code, name)` — **UNIQUE(code)**
- `city(id, country_id, name)` — **UNIQUE(country_id, name)**(city 在 data.json 里只有 name,无 code);FK→country
- `airport(id, city_id, code, name, official_url)` — **UNIQUE(code)**(IATA 全局唯一);FK→city
- `bus(id, source_id, airport_id, route, destination, operator, official_url, duration, price, operating_hours, last_updated DATE, fetch_failed, content_hash, updated_at)`
  - **`source_id VARCHAR(64)` UNIQUE** ← data.json 的 `"vie-vab1"`,导入器的幂等键 + 将来回连抓取源的锚点
  - FK→airport;**INDEX(airport_id)**
  - `last_updated` 用 `DATE`(数据是日期粒度,无时间);`fetch_failed BOOLEAN` 保留以无损承载 data.json
  - **`price / duration / operating_hours` 是 `VARCHAR(512)` 展示文本,不做结构化查询**——真实值形如「全价单程 €11 / 往返 €20」「去机场 03:00-24:00 / 返程 04:30-次日 01:00」,带方向/币种/中外文混排,强拆成数值会丢信息。若将来要按价格排序,另加派生列 `price_amount DECIMAL`,不动原文。这是有意决策。
  - `content_hash`:见下方「变更检测算法」,**必须覆盖子表**
- `bus_stop(id, bus_id, seq, name)` — stops 数组;**UNIQUE(bus_id, seq)**;FK→bus ON DELETE CASCADE;导入按数组下标(从 1)赋 seq
- `bus_schedule(id, bus_id, time_range, interval_text, note)` — **`interval` 是 MySQL 保留字,改名 `interval_text`**;FK→bus CASCADE
- `bus_image(id, bus_id, url, caption)` — FK→bus CASCADE
- `bus_file(id, bus_id, name, url)` — FK→bus CASCADE
- `bus_alert(id, bus_id, type, message, start_date DATE NULL, end_date DATE NULL)` — 日期用 `DATE` 且允许 NULL(长期提醒无起止);`type VARCHAR(32)`(info/reroute…);FK→bus CASCADE

用户与互动:
- `user(id, username, password_hash, email, nickname, avatar_url, locale, status, created_at, updated_at)` — 密码 BCrypt;**UNIQUE(username)、UNIQUE(email)**(重复注册返回 409)
- `favorite(id, user_id, bus_id, notify, created_at)` — 收藏即订阅;**UNIQUE(user_id, bus_id)**、**INDEX(bus_id)**(推送时反查订阅者的热路径);FK→user/bus。`notify BOOLEAN DEFAULT 1` 几乎零成本,留出「收藏但不打扰」的将来能力
- `message(id, user_id, type, title, template_code, params_json, ref_bus_id, ref_hash, is_read, created_at)` — 站内信;**INDEX(user_id, is_read, created_at)**;**UNIQUE(user_id, ref_bus_id, ref_hash)** 做推送幂等(管理员连点保存不重复发)。`template_code + params_json` 存模板而非定死文本,前端按 `user.locale` 渲染 → 站内信也能多语言
- `ticket(id, user_id, type, title, content, status, created_at, updated_at)` — 建议工单;`status` 状态机:`OPEN → REPLIED → CLOSED`(管理员回复置 REPLIED,任一方关闭置 CLOSED,用户再回复可从 CLOSED 重开为 OPEN)
- `ticket_reply(id, ticket_id, author_type, author_id, content, created_at)` — `author_type` ∈ {USER, ADMIN},配合 `author_id` 跨 user/admin_user 两套体系引用;FK→ticket CASCADE

后台与审计:
- `admin_user(id, username, password_hash, role, status, created_at)` — `role` ∈ {SUPER_ADMIN, OPERATOR};**初始 SUPER_ADMIN 由种子脚本/Flyway 写入**,不开放注册
- `audit_log(id, admin_id, action, target_type, target_id, detail_json, ip, created_at)` — **INDEX(admin_id, created_at)、INDEX(target_type, target_id)**

i18n(预留,先不强制):
- 界面文案走前端 vue-i18n JSON,不入库
- 数据译文若将来需要:`i18n_text(id, entity_type, entity_id, field, locale, text)` 侧表,先建表不填

### 变更检测算法(content_hash —— 对抗评审 P0,这是闭环地基)

变更最常发生在子表(加一条 alert、改一个 schedule 的班次),所以 hash **必须覆盖子表**,否则最典型的「巴士信息更新」检测不到、推送漏发。规范化算法:

```
content_hash = SHA256( canonicalJson )
canonicalJson = {
  bus 标量字段(route,destination,operator,duration,price,operating_hours,...),
  stops:      按 seq 升序的 name 列表,
  schedules:  按 (time_range,interval_text) 稳定排序,
  alerts:     按 (type,start_date,end_date) 稳定排序,
  images/files: 按 url 稳定排序
}   // key 有序、数组有序,保证两次计算结果一致
```

子表保存策略统一「先删后插」,hash 在排序后的稳定序列上计算。保存前算新 hash 与库中旧 hash 对比:**相同则无变更、不发推送**(这同时就是推送去重)。

### 核心流程:变更推送闭环

1. 管理员在后台保存某条 `bus`(含子表)→ 同一 DB 事务内写库 + 算新 `content_hash`
2. 与旧 `content_hash` 对比;**相同则结束(无变更不发)**;不同则记 `changedFields`
3. 用 **`@TransactionalEventListener(phase = AFTER_COMMIT)`** 发 `BusUpdatedEvent(busId, newHash, changedFields)` —— **bus 先提交,推送在提交后异步执行**
4. 监听器(`@Async`)查 `favorite WHERE bus_id=? AND notify=1` 找订阅者,**单条批量 `INSERT message VALUES (...),(...)`**;`UNIQUE(user_id,ref_bus_id,ref_hash)` 保证幂等(至少一次 + 去重)
5. 实时推送:见下方分期
6. 增删场景:**新增**线路默认不推送(无人订阅);**删除**被收藏的线路 → 给订阅者发一条「该线路已下线」站内信,`favorite` 随 bus `ON DELETE CASCADE` 清理(`message` 历史保留不级联删)

> **为什么不放同一事务**(修正原稿矛盾):把「给所有订阅者写信」塞进管理员保存事务里,会让保存请求随订阅者数线性变慢、长时间持锁,且任一条 message 失败会回滚掉 bus 保存(业务上错的——该存的数据没存)。AFTER_COMMIT + 异步 + 幂等去重是正确取舍:数据落地优先,通知最终一致。

**推送实时性,分两期(推荐):**
- **MVP**:站内信落库 + 未读数。前端登录后/轮询拉未读数,红点提示。简单可靠,先打通闭环。
- **Phase 2**:SSE(Server-Sent Events,比 WebSocket 轻,单向推送正好够用)做实时红点。Redis 存每用户未读计数。

### Redis 用途

- 巴士查询树缓存:粗粒度单 key `bus:tree`(读多写少),**在 AFTER_COMMIT 失效**(避免事务回滚后用旧值重建脏缓存)+ TTL 兜底防失效遗漏;读路径加空值缓存防击穿
- 登录限流;短期 access token + refresh token(**不做 JWT 黑名单**——黑名单等于给无状态 JWT 引入服务端状态,矛盾)
- 每用户未读站内信计数:**Redis 为加速缓存,MySQL `message.is_read` 为权威真相**;Redis miss 时回源 `COUNT(*) WHERE is_read=0` 重建并设 TTL;写信 `INCR`、标记已读 `DECR`,允许短暂不一致但能自愈

### 多语言

- 界面:vue-i18n,语言包 JSON(zh-CN / en / de 起步,对应已有两城)
- 用户 `locale` 字段记住偏好;`Accept-Language` 兜底
- 后端错误信息用 Spring `MessageSource` 国际化
- **站内信也要多语言**:`message` 存 `template_code + params_json`(如 `BUS_UPDATED` + `{route, changed}`),前端按当前 `user.locale` 渲染。不存定死文本,否则德/英/中用户收到的通知语言无法适配
- 数据本身先保留原文,`i18n_text` 侧表预留不实现

### 边界与状态(对抗评审补全)

- **空态**:搜其他国家/无机场/无巴士都会空(只有两城),前端统一空态组件;API 空结果返回空数组而非 404
- **重复注册**:`username/email` 唯一索引冲突 → 409,前端提示
- **站内信生命周期**:列表分页 + 批量已读 + 可删除;订阅热门线路会持续累积,设可选保留期(如 90 天归档)
- **登录安全**:Redis 登录限流;MVP 明确**不做**密码找回/邮箱验证(记为有意取舍,Phase 2 再加)
- **工单状态机**:`OPEN → REPLIED → CLOSED`,CLOSED 后用户再回复重开为 OPEN

### 管理后台

Element Plus + 同一前端工程内独立路由(`/admin/*`,基于 `admin_user` 角色鉴权),或单独 Vite 子应用——MVP 同仓同应用、路由分区即可,无需第二个部署。
- 用户注册情况:列表 + 注册趋势图
- 订阅统计:按 bus/airport/city 聚合 favorite 计数
- 巴士信息维护:树形选国家/城市/机场 → 编辑线路(保存即触发推送闭环)
- 操作记录:audit_log 列表 + 过滤

## Open Questions

1. **收藏 vs 订阅是否需要拆开?** 当前设计「收藏即订阅」。若你想要「收藏但不接收通知」或「订阅整个机场/城市而非单条线路」,需把订阅拆成独立实体(`subscription(user_id, target_type, target_id)`)。建议 MVP 先合并,确有需要再拆。
2. **管理后台是否要独立部署?** 建议 MVP 同应用路由分区;真要前台/后台独立上线再拆子应用。
3. **推送实时性 MVP 是否接受「轮询未读数」?** 还是一上来就要 SSE 实时?建议先轮询,Phase 2 上 SSE。
4. **多语言数据译文** 是否在本期范围?建议本期只做界面 i18n。

## Success Criteria

- 用户能按 国家/城市/机场 三级筛选并看到维也纳、上海的巴士线路详情
- 注册/登录/个人中心/收藏 全链路可用
- 管理员在后台改动一条被收藏的线路 → 对应用户能在站内信看到一条变更通知
- 界面可切换至少 zh-CN / en 两种语言
- 后台能看到用户数、订阅统计,且每次后台写操作都有 audit_log 记录

## Distribution Plan

Web 服务,标准部署:
- 后端打 Spring Boot 可执行 jar,Docker 镜像;前端 Vite build 出静态文件,Nginx 托管或后端静态目录
- `docker-compose`:app + mysql + redis,本地一键起
- CI/CD:GitHub Actions 构建镜像即可(MVP 手动部署亦可)

## Dependencies

- data.json 作为种子,需写一个初始化导入器(读 JSON → 落 MySQL),应用首启或独立脚本执行
- 无外部抓取依赖(本期延后)

## Next Steps(建议实现顺序)

1. **建库 + 种子导入**:定义上述 schema,写 data.json 导入器,确认两城数据正确落库
2. **巴士查询 API + 前端三级筛选页**:打通「能查到信息」这条主线(产品的地基)
3. **用户系统**:注册/登录(JWT)/个人中心/收藏
4. **站内信 + 变更推送闭环**:`BusUpdatedEvent` → 订阅者 → message(先做落库 + 未读数)
5. **管理后台**:巴士维护(接上第 4 步的推送)、用户/订阅统计、audit_log
6. **建议工单**:用户提单 + 后台回复
7. **多语言 i18n**:界面语言包 + locale 偏好
8. **打磨**:Redis 缓存、SSE 实时推送(Phase 2)、Docker 编排

## Spec Review(多 agent 对抗评审)

本文档经两个独立 reviewer agent(无对话上下文,仅看文档 + 核对真实 data.json)对抗评审一轮:
- **工程/可行性视角**:初评 5.5/10。命中:丢失 data.json 业务主键、零唯一索引/外键、content_hash 未覆盖子表、`interval` 保留字、alert 日期类型、「同事务推送」自相矛盾、JWT 黑名单与无状态矛盾。
- **产品/范围视角**:初评 7/10(范围控制是强项,反微服务/反 MQ/分期推送被点赞)。命中:工单状态机缺失、用户唯一约束、站内信生命周期、推送去重、删除/新增推送语义、站内信多语言。

**已修复**(本次修订):自然键 + 全部唯一索引/外键/热路径索引、content_hash 规范化算法(含子表)、`interval_text` 改名、DATE 类型、AFTER_COMMIT 异步幂等推送、Redis 缓存为加速 + MySQL 为真相、站内信模板化多语言、工单状态机、增删推送语义、空态/重复注册/登录安全边界。预计修复后达 8+/10,可直接开工。

**保留为有意取舍**:`fetch_failed` 保留(无损承载 data.json);`price/duration` 用字符串;收藏即订阅(留 `notify` 位);密码找回延后 Phase 2。

## The Assignment

在写任何业务代码之前,先做**第 1 步**:把上面已经定死自然键、唯一索引、外键、字段类型的 schema 落成最终 MySQL 建表 DDL(用 Flyway 管理迁移),并写出**幂等**种子导入器——以 `bus.source_id`、`country.code`、`(country_id, name)` 做 upsert,重跑两次结果一致,真正把维也纳和上海两城数据导进库里跑通。**数据模型对了,后面所有 API 和页面都顺;数据模型错了,后面全返工。** 这是整个项目风险最高、最该先锁死的一环——对抗评审的火力也全压在这里。

## What I noticed about how you think

- 你给了一份相当完整的 spec —— 数据源、功能清单、技术栈、连缓存数据库都点了名。这说明你不是「有个模糊想法」,而是已经在脑子里把系统跑了一遍。这是工程师的思维方式。
- 你主动把前端留成开放问题让我推荐,而后端却定得很死。说明你知道自己的强项边界在哪,不在不确定的地方硬撑 —— 这比假装什么都懂强。
- 当我挑战「微服务」这个前提时,你直接选了模块化单体,没有为已经写在需求里的东西辩护。能为正确的方案放弃自己原本的设定,是难得的。
